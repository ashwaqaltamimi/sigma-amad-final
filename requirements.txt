# إصدارات مثبّتة بدقة — بناء قابل للتكرار.
# سبب التثبيت: النطاقات المفتوحة (>=) كانت تُثبّت أحدث إصدار عند كل إعادة تهيئة
# للبيئة، فأي تغيير كاسر في أي مكتبة يُسقط التطبيق الحي.
#
# ⚠️ قاعدة إلزامية: أي تعديل هنا يجب أن يمرّ بتثبيت نظيف من الصفر قبل الدفع:
#     pip install --dry-run --ignore-installed --only-binary=:all: -r requirements.txt
# النسخ المأخوذة من `pip freeze` لبيئة محلية قديمة قد تكون مستحيلة التثبيت معًا
# (streamlit 1.35 مثلًا يشترط numpy<2 — فتثبيته مع numpy 2.x يُسقط البناء السحابي).
# هذه المجموعة تُحلّ نظيفة على Python 3.12 و3.13 معًا، بعجلات جاهزة لكل الحزم.
# متحقَّق منها: 41 اختباراً + تشغيل كامل للتطبيق على Python 3.13.
streamlit==1.57.0
pandas==2.3.3
numpy==2.2.6
plotly==5.22.0
scikit-learn==1.5.2
reportlab==4.5.1
arabic-reshaper==3.0.1
python-bidi==0.6.10
openpyxl==3.1.5
openai==2.37.0
