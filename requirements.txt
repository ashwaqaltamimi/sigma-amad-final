# إصدارات مثبّتة بدقة — بناء قابل للتكرار.
# سبب التثبيت: النطاقات المفتوحة (>=) كانت تُثبّت أحدث إصدار عند كل إعادة تهيئة
# للبيئة، فأي تغيير كاسر في أي مكتبة يُسقط التطبيق الحي. هذه المجموعة بالذات
# مُختبَرة ومتحقَّق من عملها كاملةً (514 عملية · الطبقات الثلاث · 41 اختباراً).
# ملاحظة نشر: تتطلب Python 3.12 (تُضبط من إعدادات التطبيق في Streamlit Cloud).
streamlit==1.35.0
pandas==2.2.2
numpy==2.2.6
plotly==5.22.0
scikit-learn==1.5.0
reportlab==4.5.1
arabic-reshaper==3.0.1
python-bidi==0.6.10
openpyxl==3.1.5
openai==2.37.0
